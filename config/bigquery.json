{
    "jobs": [
        {
            "reader_type": "rdbms",
            "reader_url": "bigquery://",

            "writer_type": "couchbase",
            "writer_url": "couchbases://XXXXXXX:XXXXXXX@localhost/test",

            "subjobs":[
                {
                    "reader_query": "select * from `XXXXXXXXXXXXXXXXXXX` limit 15000",
                    "reader_key": "{row['storeno']}::{row['businessdate']}::{row['workstationid']}::{row['cashierno']}::{row['transactionsequencenumber']}",
                    "reader_chunksize": 500,

                    "writer_scope":"care",
                    "writer_collection":"care",
                    "writer_threads":10
                }
            ]
        }
    ]
}
